# Ignore all top-level directories (like local scrape folders)
/*/

# Except the .github folder (for our GHA workflow) and org/ (pre-downloaded data)
!/.github/
!/org/

# Ignore SQLite databases, logs, caches
*.db
*.db-journal
*.db-wal
*.log
__pycache__/
results/
tender_counts_cache.json
